# ====================================
# 波形标注系统环境变量配置示例
# ====================================

# JWT 配置
# 生产环境必须修改为强密钥
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production

# 数据集配置
# 数据集存储路径（可以是绝对路径或相对路径）
DATASET_PATH=./dataset
# 也可以使用绝对路径，例如：
# DATASET_PATH=/home/user/datasets/waveform-data
# DATASET_PATH=/mnt/shared-storage/datasets

# 是否允许写入数据集（true/false）
ENABLE_DATASET_WRITE=true

# MongoDB 配置（默认使用外部数据源）
MONGODB_URI=mongodb://mongodb:Qq1970251968@117.50.201.9:27018/
# 可选：单独指定业务数据库名（不写在 URI 里也可以）
MONGODB_DATABASE=waveform-annotation-system
# 其他示例：
# MONGODB_URI=mongodb://username:password@your-mongo-host:27017/yourdb?authSource=admin

# Redis 配置（外部 Redis 服务）
REDIS_URL=redis://:Qq1970251968@117.50.201.9:26739

# CORS 配置（前端访问地址） - JSON 数组格式
CORS_ORIGINS=["http://localhost:3000", "http://frontend:80", "http://127.0.0.1:3000"]

# 容器内数据集路径（通常不需要修改）
H5_DATA_PATH=/data/dataset

# 前端 API 配置
# Vite 构建期 API 前缀（留空则使用 Nginx 反代 /api -> backend:8000）
# 例如需要前端直连后端时：VITE_API_BASE_URL=http://your-backend-host:8000
VITE_API_BASE_URL=

# ====================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据需要修改配置值
# 3. 重新启动服务: docker-compose up -d --build
# ====================================

# 常用数据集路径示例：
#
# 开发环境 - 当前目录:
# DATASET_PATH=./dataset
#
# 共享存储 - NFS/SMB:
# DATASET_PATH=/mnt/shared-datasets
#
# 外部硬盘:
# DATASET_PATH=/media/external-drive/datasets
#
# 用户主目录:
# DATASET_PATH=~/waveform-datasets
